# Git Capability

- Use read-only git inspection by default (`status`, `diff`, `branch`, `remote -v`).
- Avoid history-rewriting operations unless explicitly approved.
- Report exact file paths and concise diffs when summarizing changes.
