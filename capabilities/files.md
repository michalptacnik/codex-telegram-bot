# Files Capability

- Restrict file reads and writes to the active workspace root.
- Validate relative paths and reject traversal attempts.
- Keep write operations minimal and explain changed files clearly.
